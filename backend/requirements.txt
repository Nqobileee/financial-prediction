# Core ML and Data Processing
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.2.0
joblib>=1.2.0

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# Production WSGI Server
gunicorn>=21.2.0

# Python Standard Libraries (usually included, but good to specify versions)
# Note: os, sys, json, warnings are built-in modules